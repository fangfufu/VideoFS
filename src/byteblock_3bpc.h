#ifndef BYTEBLOCK_3BPC_H
#define BYTEBLOCK_3BPC_H
/**
 * \file byteblock_2bpc.h
 * \brief Byteblock interconversion - 3-bit colour
 * \details This compilation unit handles the conversion from a byte to a
 * byteblock using 2-bit colour representation.
 *
 * We are assuming the target colour space is RGB888.
 *
 * In this configuration, we store 3 bits per colour channel. Each pixel can
 * store 9 bit of information. However we are only using 8 bits.
 */

#include <stdint.h>


#endif // BYTEBLOCK_3BPC_H